void sendln(string msg, bool newline = false) {
    if (newline) {
        includeC ```
            printf("%s\n", msg);
        ```
    } else {
        includeC ```
            printf("%s", msg);
        ```
    }
}