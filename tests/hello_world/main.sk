use std/io

void main(arr args) {
    includeC ```
        #ifdef SK_OS_WINDOWS
            printf("Running on Windows\n");
        #elif defined(SK_OS_LINUX)
            printf("Running on Linux\n");
        #else
            printf("Unknown OS\n");
        #endif
    ```
}